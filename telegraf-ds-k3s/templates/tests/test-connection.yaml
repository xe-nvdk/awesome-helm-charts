apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telegraf-ds-k3s.fullname" . }}-test-connection"
  labels:
    {{- include "telegraf-ds-k3s.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telegraf-ds-k3s.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
